<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.renaudbaivier.sounder"
    android:versionCode="1"
    android:versionName="1.0" android:installLocation="auto">

    <uses-sdk android:minSdkVersion="8" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" >
        <activity
            android:name="PlayerActivity" android:launchMode="singleInstance"
            android:label="@string/app_name"
            android:configChanges="keyboardHidden|orientation"
            
             >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
       
	<activity android:name="TabsActivity" android:label="@string/app_name"
          android:theme="@android:style/Theme.NoTitleBar" android:launchMode="singleInstance"></activity>
	<activity android:name="PlayListsActivity" android:launchMode="singleInstance"></activity>
	<activity android:name="ListAlbumActivity" android:launchMode="singleInstance"></activity>
	<activity android:name="ListArtistActivity" android:launchMode="singleInstance"></activity>
	<activity android:name="ListTitreActivity" android:launchMode="singleInstance"></activity>
    </application>

</manifest>